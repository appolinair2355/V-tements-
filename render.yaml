# Configuration de déploiement pour Render.com
services:
  - type: web
    name: styleshop
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
